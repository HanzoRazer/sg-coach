# Developer Handoff: Smart Guitar AI Infrastructure
**Session Date:** January 24, 2026  
**Scope:** Groove Layer contracts, zt-band adapters, CI gates

---

## Executive Summary

This session established the **Groove Layer** as the canonical bridge between player behavior analysis and system control. Two formal JSON Schema contracts were shipped, along with a MIDI adapter pipeline and clock master for zt-band.

### What Was Shipped

| Artifact | Repo | Status |
|----------|------|--------|
| `groove_profile_v1.schema.json` | sg-coach | ✅ v1-locked |
| `groove_control_intent_v1.schema.json` | sg-coach | ✅ v1-locked |
| `zt_band/adapters/` (Intent → MIDI) | string_master | ✅ Merged |
| `zt_band/midi/` (Clock master) | string_master | ✅ Merged |
| Pydantic schemas in sg-spec | sg-spec | ✅ Synced |
| Golden vectors (13 vectors) | sg-coach | ✅ Merged |
| `zt_band/midi/humanizer.py` (DeterministicHumanizer) | string_master | ✅ Merged |
| RealtimeScheduler jitter integration | string_master | ✅ Merged |
| `zt_band/adapters/arranger_intent_adapter.py` | string_master | ✅ Merged |
| `zt_band/adapters/arranger_control_plan.py` | string_master | ✅ Merged |

---

## Repository Map

```
C:\Users\thepr\Downloads\
├── sg-coach/                    # Coach logic + contracts
│   ├── contracts/
│   │   ├── groove_profile_v1.schema.json
│   │   ├── groove_control_intent_v1.schema.json
│   │   └── CHANGELOG.md
│   ├── fixtures/golden/groove_vectors/  # 13 golden vectors
│   ├── src/sg_coach/
│   │   ├── groove_intent_engine_v1.py   # Profile → Intent mapper
│   │   └── groove_replay_gate_v1.py     # Replay gate with diff output
│   ├── scripts/ci/              # CI gate scripts
│   └── .github/workflows/groove_vectors_gate.yml
│
├── sg-spec/                     # Pydantic schema library
│   └── sg_spec/schemas/
│       ├── groove_layer.py      # ✅ Synced with JSON Schema
│       ├── smart_guitar.py
│       └── sandbox_schemas.py
│
└── string_master_v.4.0/         # zt-band + zone-tritone
    └── src/zt_band/
        ├── adapters/
        │   ├── groove_intent_adapter.py
        │   ├── arranger_intent_adapter.py   # NEW
        │   ├── arranger_control_plan.py     # NEW
        │   ├── midi_control_plan.py
        │   └── default_map.py
        ├── midi/
        │   ├── midi_clock.py
        │   └── humanizer.py     # NEW: DeterministicHumanizer
        └── scheduler.py         # Updated: humanizer integration
```

---

## Contract Architecture

### The Profile → Intent Boundary

```
┌─────────────────────┐         ┌──────────────────────────┐
│  GrooveProfileV1    │────────▶│  GrooveControlIntentV1   │
│  (Persistent)       │         │  (Ephemeral)             │
│                     │         │                          │
│  "What kind of      │         │  "What should the        │
│   groove player     │         │   system do RIGHT NOW?"  │
│   is this?"         │         │                          │
└─────────────────────┘         └──────────────────────────┘
        ▲                                   │
        │                                   ▼
   Groove Layer                    ┌─────────────────┐
   (Analyzer)                      │  MidiControlPlan │
                                   │  (zt-band)       │
                                   └─────────────────┘
```

### Key Design Properties

| Property | Implementation |
|----------|----------------|
| **No learning state leaks** | Intent is ephemeral (horizon_ms bounds validity) |
| **Latency-safe** | horizon_ms: 50–60000ms |
| **Testable** | Pure input/output contracts |
| **Composable** | zt-band, Coach, Arranger all consume Intent |
| **Upgradeable** | `extensions` namespace for additive fields |

---

## Schema Reference

### groove_profile_v1.schema.json

**SHA256:** `sha256:afa948a4c6e52b0713d2df11a3a948342c9423fd864188a0c99cf753de57f7c4`

```json
{
  "schema_id": "groove_profile",
  "schema_version": "v1",
  "profile_id": "gp_abc123",
  "scope": "device_local",
  "timing_bias": {
    "mean_offset_ms": -12.5,
    "stddev_ms": 8.0,
    "direction": "ahead",
    "confidence": 0.85
  },
  "tempo_stability": {
    "supported_bpm_range": [80, 140],
    "drift_slope": 0.3,
    "fatigue_sensitivity": 0.4,
    "confidence": 0.9
  },
  "subdivision_fidelity": {
    "supported": ["quarter", "eighth"],
    "unstable": ["triplet_sixteenth"],
    "swing_tolerance": 0.6,
    "confidence": 0.8
  },
  "error_recovery": {
    "mean_recovery_beats": 2.5,
    "panic_probability": 0.15,
    "self_correction_rate": 0.7
  },
  "groove_elasticity": {
    "microtiming_flex_ms": 25.0,
    "lock_threshold": 0.7,
    "push_pull_balance": "push"
  },
  "confidence_band": { "lower": 0.75, "upper": 0.92 },
  "evidence_window": { "sessions": 15, "events": 4200 }
}
```

### groove_control_intent_v1.schema.json

**SHA256:** `sha256:3659ba6528e0b9174b006e7534e716c37be5eedee5ae1669cbe95a355d35cb4f`

```json
{
  "schema_id": "groove_control_intent",
  "schema_version": "v1",
  "intent_id": "gci_000001",
  "profile_id": "gp_abc123",
  "generated_at_utc": "2026-01-24T10:30:00Z",
  "horizon_ms": 2000,
  "confidence": 0.85,
  "control_modes": ["stabilize"],
  "tempo": {
    "target_bpm": 92,
    "lock_strength": 0.75,
    "drift_correction": "soft"
  },
  "timing": {
    "microshift_ms": -18.0,
    "anticipation_bias": "ahead"
  },
  "dynamics": {
    "assist_gain": 0.6,
    "expression_window": 0.4
  },
  "recovery": {
    "enabled": true,
    "grace_beats": 2.0
  },
  "reason_codes": ["tempo_drift"]
}
```

---

## zt-band Adapter Pipeline

### Intent → MIDI Control Plan

```python
from zt_band.adapters import build_midi_control_plan

intent = load_intent()  # GrooveControlIntentV1 dict
plan = build_midi_control_plan(intent)

# plan.clock_mode      → "midi_clock_master" | "midi_clock_follow"
# plan.target_bpm      → 92.0
# plan.humanize_ms     → 7.5 (derived from expression_window * (1 - lock_strength))
# plan.global_microshift_ms → -18.0 (clamped to ±30ms)
# plan.cc_messages     → [(0xB0, 20, 95), (0xB0, 21, 32), ...]
```

### MIDI CC Mapping (default_map.py)

| CC | Parameter | Range |
|----|-----------|-------|
| CC20 | Tightness (lock_strength) | 0–127 |
| CC21 | Humanize | 0–127 |
| CC22 | Assist Gain | 0–127 |
| CC23 | Expression Window | 0–127 |
| CC24 | Drift Correction | 0/64/127 (none/soft/aggressive) |
| CC25 | Recovery Enable | 0/127 (off/on) |

### Clock Mode Selection

| control_modes contains | clock_mode |
|------------------------|------------|
| `follow`, `assist` | `midi_clock_follow` |
| `stabilize`, `challenge`, `recover` | `midi_clock_master` |

---

## MIDI Clock Master

```python
from zt_band.midi import MidiClockMaster

def my_sender(data: bytes) -> None:
    midi_port.send(data)

clock = MidiClockMaster(send_bytes=my_sender)
clock.start(bpm=120.0)

while running:
    clock.set_bpm_target(plan.target_bpm)  # From Groove Control Intent
    clock.tick()                           # Call every 1-5ms
    time.sleep(0.002)

clock.stop()
```

### TempoSmoother Behavior

- **tau_s=0.25**: Exponential smoothing time constant
- **max_slew_bpm_per_s=50.0**: Hard rate limit
- **Bounded catch-up**: max_catchup_ticks=6 (prevents runaway if loop overruns)

---

## Deterministic Humanizer (zt-band)

A seedable deterministic jitter generator was added to remove "random feel" nondeterminism while preserving musical looseness.

### Module

`src/zt_band/midi/humanizer.py` → `DeterministicHumanizer`

### Integration Point

`RealtimeScheduler.run()` dispatch-time target computation

### Key Properties

- **Hash-based**: `sha256(seed|channel|tick_index)` — stable across OS/Python versions
- **Two modes**: `"white"` (independent per tick) or `"smooth"` (value-noise interpolation)
- **Bounded**: Output always in `[-humanize_ms, +humanize_ms]`
- **Auto-disable**: When `lock_strength >= 0.95`, `humanize_enabled=False`

### Usage

```python
from zt_band.midi import DeterministicHumanizer
from zt_band.scheduler import RealtimeScheduler

humanizer = DeterministicHumanizer(seed=plan.humanize_seed, mode="smooth")
sched = RealtimeScheduler(
    sender=my_sender,
    humanizer=humanizer if plan.humanize_enabled else None,
    humanize_ms=plan.humanize_ms if plan.humanize_enabled else 0.0,
)
sched.run(events, bpm=plan.target_bpm, ticks_per_beat=480)
```

### Commit

`943f637` (humanizer) → `248e2ba` (scheduler integration)

---

## Arranger Adapter (zt-band)

A canonical bridge from `GrooveControlIntentV1` to arranger-facing controls (style/density/pattern selection).

### Module

`src/zt_band/adapters/arranger_intent_adapter.py` → `build_arranger_control_plan()`

### ArrangerControlPlan Fields

| Field | Type | Source |
|-------|------|--------|
| `mode` | `follow\|assist\|stabilize\|challenge\|recover` | Priority selection from control_modes |
| `density` | `sparse\|normal\|dense` | Mode-based: recover→sparse, challenge→dense |
| `energy` | `low\|mid\|high` | `0.6*assist_gain + 0.4*(1-tightness)` |
| `pattern_family` | `straight\|swing\|shuffle\|free` | Mode + expression_window threshold |
| `tightness` | `float 0..1` | `tempo.lock_strength` |
| `expression_window` | `float 0..1` | `dynamics.expression_window` |
| `assist_gain` | `float 0..1` | `dynamics.assist_gain` |
| `anticipation_bias` | `ahead\|behind\|neutral` | `timing.anticipation_bias` |

### Mode Priority

`recover > challenge > stabilize > assist > follow`

### Usage

```python
from zt_band.adapters import build_arranger_control_plan

plan = build_arranger_control_plan(intent)
# plan.mode          → "stabilize"
# plan.density       → "normal"
# plan.pattern_family → "straight"
```

### Commit

`248e2ba`

---

## CI Gates (sg-coach)

| Gate | Script | Purpose |
|------|--------|---------|
| Contracts Governance | `scripts/ci/check_contracts_governance.py` | SHA256 validation, v1 immutability |
| Vectors Complete | `scripts/ci/check_sg_coach_vectors_complete.py` | Golden fixtures present |
| Replay Determinism | `scripts/ci/check_sg_coach_replay_determinism.py` | Byte-identical replay outputs |

### Bootstrap Sentinel

File `.sg_coach_bootstrap` allows temporary bypass of vector gates during initial setup.  
**CRITICAL:** Sentinel is forbidden on `main`/`master` branches.

---

## Pending Work

### Completed This Session

1. ~~**Sync sg-spec Pydantic models** with JSON Schema~~ ✅
   - `sg-spec/sg_spec/schemas/groove_layer.py` now matches JSON Schema contracts
   - 6 sync tests passing

2. ~~**Golden vector set** for Profile → Intent~~ ✅
   - 13 vectors in `fixtures/golden/groove_vectors/`
   - Replay gate with diff output and changelog enforcement
   - CI gates wired

3. ~~**Deterministic humanizer** (Ship B)~~ ✅
   - `DeterministicHumanizer` with white/smooth modes
   - Hash-based (not PRNG) for cross-platform stability
   - Wired into `RealtimeScheduler.run()` dispatch-time target
   - Commit: 943f637

4. ~~**Arranger adapter stub** (Ship C)~~ ✅
   - `ArrangerControlPlan` with mode/density/energy/pattern_family
   - `build_arranger_control_plan()` deterministic mapper
   - 22 tests passing
   - Commit: 248e2ba

### High Priority (Remaining)

1. **Wire ArrangerControlPlan into arranger engine**
   - Bridge to `engine.py` pattern selection
   - Integration test: Intent → ArrangerControlPlan → pattern selection

2. **Add golden fixture for arranger mapping**
   - One intent JSON in, one ArrangerControlPlan JSON out
   - Match existing groove vector governance style

### Medium Priority

3. **Wire contracts governance gate** to CI
   - Validate SHA256 of locked schemas on PR

---

## Code Generation Patterns

### When creating new contracts

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sg-ai:contracts/<name>_v1.schema.json",
  "additionalProperties": false,
  "required": ["schema_id", "schema_version", ...],
  "properties": {
    "schema_id": { "type": "string", "const": "<name>" },
    "schema_version": { "type": "string", "const": "v1" },
    "extensions": {
      "type": "object",
      "additionalProperties": true,
      "description": "Forward-compatible extension space."
    }
  }
}
```

### When adding zt-band adapters

```python
# Location: src/zt_band/adapters/<name>.py
# Import pattern:
from zt_band.adapters.midi_control_plan import MidiControlPlan
from zt_band.adapters.default_map import DEFAULT_MAP
```

### When adding tests

```python
# Location: tests/test_<module>.py
# Import from installed package:
from zt_band.adapters import build_midi_control_plan
from zt_band.midi import MidiClockMaster
```

---

## Terminology (Frozen)

| Term | Definition |
|------|------------|
| **Groove Profile** | Persistent rhythmic personality traits (HOW player grooves) |
| **Control Intent** | Ephemeral prescriptive output (WHAT system does next) |
| **horizon_ms** | Validity window for intent before regeneration |
| **lock_strength** | 0=free follow, 1=hard lock to grid |
| **control_modes** | Active strategies: follow, assist, stabilize, challenge, recover |
| **drift_correction** | none, soft, aggressive — tempo drift handling |

---

## Git Commits This Session

### sg-coach
```
f784e12 feat(groove-vectors): add 10 edge case vectors (drift/lock/microshift/horizon boundaries)
xxxxxxx feat(groove-vectors): add golden vector infrastructure + replay gate
38d2579 feat(contracts): add groove_control_intent_v1 schema
2e9a335 feat(contracts): add groove_profile_v1 schema with governance
```

### string_master_v.4.0
```
248e2ba feat(zt-band): add Arranger Intent adapter stub
943f637 feat(scheduler): wire humanizer into RealtimeScheduler
db969c5 feat(zt-band): add seedable deterministic humanizer
32994d1 feat(zt-band): add MIDI Clock master with tempo smoothing
d47cefe feat(zt-band): add Groove Intent -> MIDI Control Plan adapter
```

### sg-spec
```
xxxxxxx feat(schemas): sync groove_layer.py with JSON Schema contracts
```

---

## Quick Start for Next Session

```bash
# sg-coach (86 tests)
cd C:\Users\thepr\Downloads\sg-coach
pip install -e ".[dev]"
pytest

# string_master (458 tests)
cd C:\Users\thepr\Downloads\string_master_v.4.0
pip install -e .
pytest tests/test_groove_intent_adapter.py tests/test_arranger_intent_adapter.py tests/test_deterministic_humanizer.py -v

# sg-spec (6 groove sync tests)
cd C:\Users\thepr\Downloads\sg-spec\sg_spec
pip install -e .
pytest tests/test_groove_schema_sync.py -v
```

---

## Contact / Context

- **GitHub Remote (sg-coach):** `https://github.com/HanzoRazer/sg-coach.git`
- **GitHub Remote (string_master):** `https://github.com/HanzoRazer/string_master_v.4.0.git`
- **Python Version:** 3.14 locally, 3.11 in CI
- **Pydantic Version:** v2

---

*Generated by Copilot session on 2026-01-24*
